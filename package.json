{
    "name": "@two-way/repo",
    "version": "0.1.6",
    "scripts": {
        "build": "concurrently \"pnpm --dir shared build\" \"pnpm --dir server build\" \"pnpm --dir client build\""
    },
    "dependencies": {
        "concurrently": "^8.2.2"
    }
}
